# Reads the candidacy info at municipality level files

Reads the candidacy info at municipality level files

## Usage

``` r
read06(file, tempd)
```

## Arguments

- file:

  Path to the .DAT file that begins with 06.

  @return data.frame

# Cleans up the files and directories created during the process

Cleans up the files and directories created during the process

## Usage

``` r
cleanup(dirs)
```

## Arguments

- dirs:

  The directories to remove.
